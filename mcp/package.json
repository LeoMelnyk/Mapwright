{
  "name": "mapwright-mcp",
  "version": "1.0.0",
  "description": "MCP server for the Mapwright dungeon editor — exposes the full editor API to Claude and other MCP clients",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "engines": {
    "node": ">=18"
  }
}
